<div class="row">
    @foreach (var user in ViewBag.UsersList)
    {
        <div class="col-4 mb-4">
            <div class="card text-light" style="height:100%; width: 100%; background-color: #333;">

                <div class="card-body d-flex justify-content-between flex-column">
                    <div>
                        <p><i class="bi bi-person"></i> UserName: @user.UserName</p>

                        <p><i class="bi bi-envelope"></i> Email: @user.Email</p>

                        @if (@user.FirstName != null)
                        {
                            <p><i class="bi bi-person"></i> Nume: @user.FirstName</p>
                        }
                        @if (@user.LastName != null)
                        {
                            <p><i class="bi bi-person"></i> Prenume: @user.LastName</p>
                        }

                    </div>
                    <div class="d-flex flex-row justify-content-between p-3">

                        <div> <a class="btn btn-sm btn-success" href="/Users/Edit/@user.Id"> Editare User</a> </div>

                        <div> <a class="btn btn-sm btn-success" href="/Users/Show/@user.Id"> Vizualizare User</a> </div>

                    </div>
                </div>
            </div>
        </div>
    }
</div>
